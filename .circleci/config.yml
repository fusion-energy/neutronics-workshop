# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details


version: 2.1
# orbs:
  # codecov: codecov/codecov@1.0.5
jobs:
  build:
    docker:
      - image: openmcworkshop/openmc_dagmc_nndc_dependencies
      
      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: run tests
          command: 
            no_output_timeout: 30m
            # python paramak/setup.py install
            pytest tests -v --cov=openmc_workshop  --cov-report term --cov-report html:htmlcov --cov-report xml --junitxml=test-reports/junit.xml
            # pytest --cov=paramak --cov-report=xml
            # mkdir -p /root/repo/test-reports
            # pip3 install coverage --user
            # pip3 install pytest-cov
            # pytest --cov=./
            # py.test --cov-report=xml --cov=paramak ./
            # py.test -v
            # cp coverage.xml /root/repo/test-reports
      - store_test_results:
          path: test-reports

      - store_artifacts:
          path: test-reports
          # destination: test-reports
      - run:
          name: install curl
          command:
            apt-get install curl
      - run: curl -s https://codecov.io/bash | bash
      # - codecov/upload:
          # file: {{ coverage_report_filepath }}
